# AGENTS.md

## 파일 간 의존 규칙

- 같은 패키지 안이라도 파일 간 암묵적 의존은 단방향이어야 한다.
  두 파일이 서로를 참조하는 구조를 만들지 않는다.

## 파일 안 함수 배치 순서

- top-down으로 배치한다: 읽는 사람이 관심 있을 함수가 위, 그 함수들이 사용하는 내부 helper는 그 아래에 둔다.
- 테스트 파일도 같다: 테스트 함수가 위, fixture 로딩·golden 비교 같은 helper는 파일 아래쪽에 둔다.
